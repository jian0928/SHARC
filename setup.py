from setuptools import find_packages, setup

setup(
    name='sharc',
    version='0.1.0',
    description='Single-image 3D human reconstruction',
    packages=find_packages(),
    python_requires='>=3.9',
    install_requires=[],
)
